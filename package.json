{
  "name": "project-colors",
  "displayName": "Workspace Name & Colors",
  "description": "Customize workspace colors and specify a name to distinguish multiple windows. Keywords: project workspace colors peacock colorize",
  "version": "1.0.7",
  "engines": {
    "vscode": "^1.60.0"
  },
  "homepage": "https://github.com/lennardv2/project-colors",
  "icon": "img/logo.png",
  "publisher": "lennardv",
  "repository": {
    "type": "git",
    "url": "https://github.com/lennardv2/project-colors"
  },
  "categories": [
    "Themes",
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "Project Colors",
      "properties": {
        "projectColors.name": {
          "type": "string",
          "description": "The name of the current project."
        },
        "projectColors.mainColor": {
          "type": "string",
          "default": "#681DD7",
          "description": "The main color used for project theming."
        },
        "projectColors.isActivityBarColored": {
          "type": "boolean",
          "default": false,
          "description": "Whether the activity bar should be colored with the main color."
        },
        "projectColors.isTitleBarColored": {
          "type": "boolean",
          "default": false,
          "description": "Whether the title bar should be colored with the main color."
        },
        "projectColors.isStatusBarColored": {
          "type": "boolean",
          "default": false,
          "description": "Whether the status bar should be colored with the main color."
        },
        "projectColors.isProjectNameColored": {
          "type": "boolean",
          "default": true,
          "description": "Whether the project name should be colored with the main color."
        },
        "projectColors.isActiveItemsColored": {
          "type": "boolean",
          "default": true,
          "description": "Whether the active items should be colored with the main color."
        },
        "projectColors.setWindowTitle": {
          "type": "boolean",
          "default": true,
          "description": "Whether the window title should be set to the project name."
        }
      }
    },
    "commands": [
      {
        "command": "project-colors.openSettings",
        "title": "Customize Project Colors"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.17.12",
    "@types/vscode": "1.60",
    "@typescript-eslint/eslint-plugin": "^8.17.0",
    "@typescript-eslint/parser": "^8.17.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0",
    "typescript": "^5.7.3"
  }
}
